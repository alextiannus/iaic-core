const id={type:'string',pattern:'^[0-9a-fA-F-]{36}$'};
export function recurringTools(recurring,actor,taskSchema){return [
 {name:'my_create_recurring_assistant_task',description:'Persist a fixed-interval goal. Missed intervals coalesce to the latest due occurrence, recording skipped count. Occurrences are independent tasks and may overlap. Model and platform allowance rules apply at execution. No calendar/timezone or UI is supplied.',inputSchema:{type:'object',properties:{requestKey:{type:'string',minLength:1,maxLength:200},firstAt:{type:'string'},intervalSeconds:{type:'integer',minimum:60,maximum:31536000},occurrenceCount:{type:['integer','null'],minimum:1,maximum:1000000},input:{...taskSchema,required:[...new Set([...taskSchema.required,'allowedTools'])]}},required:['requestKey','firstAt','intervalSeconds','input'],additionalProperties:false},handler:input=>recurring.create(actor,input)},
 {name:'my_list_recurring_assistant_tasks',description:'List recurring rules and their last durable occurrence receipt.',inputSchema:{type:'object',properties:{after:id,limit:{type:'integer',minimum:1,maximum:50}},additionalProperties:false},handler:input=>recurring.list(actor,input)},
 {name:'my_read_recurring_assistant_task',description:'Read a recurring rule, pending occurrence, skipped count and current revision.',inputSchema:{type:'object',properties:{id},required:['id'],additionalProperties:false},handler:input=>recurring.get(actor,input.id)},
 {name:'my_change_recurring_assistant_task',description:'Pause, resume/retry or permanently cancel future occurrences using the current revision. Existing scheduled intents and Tasks are not cancelled. An unresolved occurrence must be recovered before pause/cancel.',inputSchema:{type:'object',properties:{id,state:{enum:['active','paused','cancelled']},expectedRevision:{type:'integer',minimum:1}},required:['id','state','expectedRevision'],additionalProperties:false},handler:input=>recurring.change(actor,input)}
];}
