import {defineCapability} from '../capabilities/index.js';
export function createObservationCapabilities({observation,prefix='observation'}){
 const key={type:'string',minLength:1,maxLength:500},digest={type:'string',pattern:'^[a-f0-9]{64}$'};
 return [['record',{sourceId:key},['sourceId']],['assess',{channel:key,releaseId:key,manifestDigest:digest},['channel','releaseId','manifestDigest']],['protect',{assessmentId:digest,expectedRevision:{type:'integer',minimum:1,maximum:2147483646}},['assessmentId','expectedRevision']]].map(([action,properties,required])=>defineCapability({name:prefix+'.'+action,description:`${action} release observations using current host authorization and frozen policy. Trusted source references supply metrics; the Agent cannot submit grades or lower thresholds. Assessment does not itself change a release.`,input:{type:'object',properties,required,additionalProperties:false},output:{type:'object'},effect:'write',retry:'never-replay',authorize:async()=>true,implementation:{kind:'function',execute:(input,{actor})=>observation[action](actor,input)}}));
}
