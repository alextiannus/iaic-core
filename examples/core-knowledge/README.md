# Independent Knowledge example

Install the Core tarball, then run `run.mjs` with Node 20. It creates temporary reference files and verifies sourced discovery, versioned read, access denial and corrected-source invalidation. It uses no application database or ERP. `npm run verify:core-package` installs and runs it alongside the existing independent examples.
