const id={type:'string',pattern:'^[0-9a-fA-F-]{36}$'},seq={type:'integer',minimum:0,maximum:2147483646},key={type:'string',minLength:1,maxLength:200};
export function sessionTools(sessions,actor){return [
 {name:'my_set_assistant_session_state',description:'Close or reopen message intake using a stable key and current sequence. Historical snapshots and independent tasks remain available; this does not cancel tasks or delete data.',inputSchema:{type:'object',properties:{sessionId:id,state:{type:'string',enum:['open','closed']},requestKey:key,expectedSequence:seq},required:['sessionId','state','requestKey','expectedSequence'],additionalProperties:false},handler:input=>sessions.setState(actor,input)},
 {name:'my_create_assistant_session',description:'Create a scoped conversation timeline with a stable requestKey. Does not start inference.',inputSchema:{type:'object',properties:{title:{type:'string',maxLength:200},requestKey:key},required:['requestKey'],additionalProperties:false},handler:input=>sessions.create(actor,input)},
 {name:'my_list_assistant_sessions',description:'List this Assistant scoped conversation timelines.',inputSchema:{type:'object',properties:{after:id,limit:{type:'integer',minimum:1,maximum:50}},additionalProperties:false},handler:input=>sessions.list(actor,input)},
 {name:'my_read_assistant_session',description:'Read conversation events through an explicit sequence snapshot. Task references are not copies of business facts.',inputSchema:{type:'object',properties:{sessionId:id,after:seq,throughSequence:seq,limit:{type:'integer',minimum:1,maximum:50}},required:['sessionId'],additionalProperties:false},handler:input=>sessions.read(actor,input)},
 {name:'my_append_assistant_message',description:'Append a user statement using expectedSequence and a stable requestKey. It does not become a memory or grant authority.',inputSchema:{type:'object',properties:{sessionId:id,text:{type:'string',minLength:1,maxLength:8000},requestKey:key,expectedSequence:seq},required:['sessionId','text','requestKey','expectedSequence'],additionalProperties:false},handler:input=>sessions.appendMessage(actor,input)}
 ];}
